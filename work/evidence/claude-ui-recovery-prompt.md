# UA-0043 — actual Claude Sonnet 5 High UI implementation

Work only in F:/dev/test-useagent. You are the real Claude worker. UA-0043 was successfully claimed for claude by supervisor. Read AGENTS.md, knowledge/INDEX.md, knowledge/ui-design-system.md and `python tools/useagent.py task show UA-0043`. Do not read the full registry/history. Use UI/UX Pro Max skill at C:/Users/THUANLYT/.codex/skills/ui-ux-pro-max/SKILL.md and existing design-system/osblog/MASTER.md. Runtime Astra stopped with quota error; supervisor has the exclusive backend recovery scope. Do not retry Astra.

Write ONLY src/app, src/styles.css, src/main.tsx, public/assets, public/favicon.svg, design-system. Use apply_patch for edits. Do not touch backend, package files, docs, README, config, entry-server, external apps or git. Existing src/app/types.ts is the wire format; keep it compatible. Installed react-markdown + remark-gfm + testing-library + lucide NOT installed. Do not add dependencies. Use existing fonts. Implement a polished, modern minimalist editorial blog and truly usable admin, not placeholders or a demo UI. Source-derived vector illustrations permitted; do not invent product screenshots. No mocked production data.

## Integration contract (supervisor is implementing these now)

Export `App({ initialData }: { initialData: PageData })` from src/app/App.tsx. Server renders `<StaticRouter location={data.path}><App initialData={data}/></StaticRouter>`. Browser main.tsx hydrates root using JSON in `<script id="osblog-data" type="application/json">`, wrapping BrowserRouter. Full page `<a href>` navigation is preferred for simplicity and fresh SSR data; do not rely on a client loader that duplicates SSR fetches. If the data script is absent show explicit error, never fake home data. Public data is server-loaded; no effects to reload initial home/article.

PageData from types.ts: kind home/archive/article/docs/doc/about/login/admin/not-found/error, path, lang, title/description/status, posts/categories/years/query/page/limit/total, post/comments/related, docs/doc, adminEmail. `?lang=en|vi` retained in links/forms. Docs Markdown links docs/<slug>.md or relative `<slug>.md` map to /docs/<slug>?lang; vi paths likewise, root README links map to GitHub. Use safe GFM Markdown WITHOUT rehypeRaw; safe external links rel noopener noreferrer. Accessible overflow for code/table.

All non-auth API responses `{data: T, error: null, requestId}` or `{data:null,error:{code,message,fields?},requestId}`. API errors must surface; keep unsaved editor text on conflict or server failure. Same-origin fetch credentials, JSON content-type, browser provides Origin automatically. Auth endpoints return native Better Auth shape, not envelope.

- GET /api/posts?sort=latest|popular|random&q=&category=&year=&page=1&limit=20 -> data {posts:Post[],total:number}; GET /api/posts/slug/:slug -> data Post; GET /api/categories -> data Category[].
- GET /api/admin/session -> data {email:string}; unauthorized 401; POST /api/auth/sign-in/email body {email,password} -> native Better Auth; POST /api/auth/sign-out body {}. Public signup disabled. Admin SSR redirects guests to /admin/login.
- GET /api/admin/posts -> data Post[] (all statuses); GET /api/admin/posts/:id -> data Post with body; POST /api/admin/posts -> data Post; PATCH /api/admin/posts/:id -> data Post; DELETE same path body {expectedUpdatedAt} -> archived Post.
- Post payload EXACT fields: categoryId UUID; slug /^[a-z0-9]+(?:-[a-z0-9]+)*$/ max180; titleEn/Vi required max240; excerptEn/Vi required max1000; bodyEn/Vi required max100000; status draft|published|archived; publishedAt ISO or null (required when published); coverImageUrl HTTP(S) URL or /assets/ path or null; coverImageAltEn/Vi max240 (both required when cover set); seoTitleEn/Vi max240; seoDescriptionEn/Vi max320. PATCH also expectedUpdatedAt ISO. Server returns 409 CONFLICT for stale update and SLUG_TAKEN for duplicate slug. Display friendly fields and preserve edits. Bilingual editor tabs, Markdown toolbar (headings,bold,italic,link,image,list,quote,code), edit/preview/split modes, word count, actual required field feedback, localStorage draft recovery keyed by post+admin identity (clear on success/signout), beforeunload if dirty, explicit Save draft / Publish controls, custom slug and live preview URL, cover/alt controls and SEO preview. Do not force destructive reload on conflict. Localized equivalents required.
- GET /api/admin/categories -> data Category[] including archived. POST /api/admin/categories -> data Category. PATCH /api/admin/categories/:id full fields + expectedUpdatedAt. DELETE same path expectedUpdatedAt -> archived Category. Fields slug max160, nameEn/Vi max160, descriptionEn/Vi nullable, isArchived boolean. Archive is recoverable, hides category posts; clearly explain consequences and confirm in app UI.
- GET /api/comments?postId=UUID -> data PublicComment[]. GET /api/comments/token -> data {formToken}. POST /api/comments {postId,email,body,formToken,honeypot} -> data {accepted:true}; email/body required, privacy text, honeypot hidden/accessibility excluded. Always pending moderation, email never public. Refresh token on expired token without losing text.
- GET /api/admin/comments -> data AdminComment[]. PATCH /api/admin/comments/:id {status:pending|approved|rejected|spam,expectedUpdatedAt,reason?,body?} -> data {id,status,updatedAt}; DELETE same with expectedUpdatedAt -> {deleted:true}. Moderation queues, clear statuses, confirmation before delete.
- POST /api/posts/:id/view {} -> data {counted:boolean}; only article mount, best-effort, server deduplicates by hashed IP daily; don't block article.

Public UI: editorial hierarchy, bilingual nav/language link, light/dark preference with reduced-motion safe styling, search/category/year/sort/page controls using actual routes/query, article thumbnail+date/read-time/category, readable GFM typography, related articles, privacy-safe comments. Share/copy link only local clipboard, never auto-post. Docs integrated sidebar+current article+mobile nav; about explains open source blog MIT with repo/docs links. No unsupported huge feature additions.

Admin UI: login, overview/posts, create/edit route `/admin/posts/new` and `/admin/posts/:id/edit`, categories, moderation, logout. Clear loading/empty/error states and keyboard focus. 44px touch controls, input labels, aria-live errors, semantic headings. Verify mobile with tests if feasible. Write focused React interaction tests under src/app including Markdown injection refusal and draft/conflict retention. No shell/browser automation for private services. Record full changed files and checks via `python tools/useagent.py task report UA-0043 --agent claude --result completed ...` ONLY when working tests pass; report blocked with details if not. Never mark done. Do not spend tokens reading unrelated historical work. Backend may be changing concurrently; report exact integration mismatch instead of editing it.
