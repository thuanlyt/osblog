# UA-0045 — Claude Sonnet 5 High documentation reconciliation

Work ONLY in F:/dev/test-useagent, only docs/, README.md, README.vi.md, CHANGELOG.md, CONTRIBUTING.md, SECURITY.md. UA-0045 is claimed for agent claude-docs. Read AGENTS.md, knowledge/INDEX.md, and `python tools/useagent.py task show UA-0045`. Do not read full registry or historical reports. Your earlier docs candidate UA-0042 was written during backend replacement and is now outdated. Another actual Claude worker owns src/app; supervisor owns src/server/build/tests/deployment. Do not modify those paths. Use apply_patch for file edits. No new agents, no git operations, no deployments, no external UI.

Update all public-facing documentation to the CURRENT implemented product, not the old scaffold. Read focused source: package.json; src/server/env.ts, router.ts, provision.ts; tools/server/database.ts, start.ts, configure-vercel.ts; tools/build/build.ts; vercel.json; netlify.toml; src/app/admin/AdminPostEditorPage.tsx and related UI labels, src/app/pages/DocsPage.tsx. Existing docs are source-embedded in Vite SSR: /docs and /docs/<slug>, ?lang=en|vi; English docs/<slug>.md and Vietnamese docs/vi/<slug>.md; optional frontmatter title/description/order. Keep a concise ordered manual rather than a dump of internal workflow files.

Important verified state as of 2026-09-05: 47 unit/component/SQL integration tests pass; lint/typecheck now pass with at most an in-progress UI warning. A true modern admin UI is present (Markdown toolbar/edit-preview-split, EN/VI field tabs, slug, cover URL+alt, SEO, date/status, draft localStorage recovery and optimistic-concurrency UX); all need final browser QA still. Real Neon Free database osblog-db in Singapore is provisioned and linked to production only; four migrations 0000–0003 ran and replay was idempotent; Better Auth issuer field fixed; admin bootstrapped; three bilingual intro posts seeded. Local built SSR HTTP200 has actual article data and hashed assets. Vercel PROJECT exists but application is NOT YET deployed; primary osblog.thuanlyt.id.vn and secondary osblog.vercel.app remain unverified. Do not advertise a verified live demo until supervisor supplies evidence. Browser E2E with video is being added; Cap recording does not exist yet. No fake screenshots/GIF/video, placeholders or guessed filenames. Explain evidence gaps succinctly, without repeating obsolete 'no DB configured' claims. Never read/quote .env.production.local or draft/admin-access.json.

Actual commands: npm ci; npm run db:migrate (reads NODE_ENV mode or --mode=production); npm run db:bootstrap (requires OSBLOG_ADMIN_PASSWORD env, min12 chars; never overwrites existing admin); npm run db:seed (optional, idempotent published bilingual intro content); npm run dev (Vite middleware+Node SSR port5173); npm run build (dist/client hashed assets without index.html + dist/server/index.js); npm start / npm run preview (production Node HTTP bound to127.0.0.1; reverse proxy needed for VPS). Vercel single api/index.ts with included dist/server; Netlify single Fetch function with catch-all redirect and same built app. Neither migrations nor seeds run in build/deploy automatically. Explain set NODE_ENV=production and HTTPS origin, pg remote TLS, separate preview/dev databases, backups before migration and operational rollback.

ENV: DATABASE_URL pooled Postgres; DATABASE_URL_MIGRATIONS direct or DATABASE_URL_UNPOOLED fallback being aligned; BETTER_AUTH_SECRET >=32; BETTER_AUTH_URL and SITE_URL primary origin; VITE_SITE_URL public legacy/browser-safe origin; ADMIN_EMAIL allow-listed operator account; COMMENT_EMAIL_ENCRYPTION_KEY 32 random bytes base64; TRUST_PROXY=false by default (only true behind trusted reverse proxy). No public signup/mail verification/reset emails are implemented: bootstrap operator attests owner-controlled identity; credentials must be kept secret; don't invent delivery. Comments use encrypted email, signed token, honeypot, durable IP/email rate limits and pending moderation. TURNSTILE_SECRET_KEY is currently UNUSED: don't claim CAPTCHA protection. Post/category Delete UI is recoverable archive; comment delete is actual permanent deletion. No slug redirect implemented: clearly caution changing published slugs breaks existing URLs. Scheduled publishedAt is filtered in public SQL. Request bodies bounded; SQL mutations+audit transactional.

Make EN/VI README polished but honest: intro, actual capabilities, screenshots/media section linking docs/media until real assets supplied, goal setup sample, quickstart, architecture diagram if helpful, docs map, deployment links, support/contributing/security/MIT. Bilingual docs: quickstart/config/editor/admin-comments/deployment local+VPS Nginx example+Vercel+Netlify/backups+rollback/architecture/intro/changelog/media. Eliminate stale statements that router/api/index.ts or admin doesn't exist. Explain provider verification separate from adapter implementation; don't claim Netlify/VPS actual deployment. Use public GitHub ABSOLUTE URLs for links to source files/knowledge/work (relative ../src/... breaks in web docs); only local sibling doc links should use relative .md. Preserve headings with clear anchor names. Build guide must explain separate asset/server outputs and why static-only hosting isn't enough. Do not suggest pushing secrets or personal draft files. Media instructions must say Cap pending; browser recordings only if supervisor supplies actual assets.

At end, run link/source consistency checks without writing out of scope. Report UA-0045 via CLI as claude-docs with changed file paths and exact checks; never mark done, no claim independent release review. Leave only real live/media/final QA limitations, not phantom implementation blockers. Keep final summary concise.
