# UA-0046 — screenshot-backed UI polish, Claude Sonnet 5 High

Work F:/dev/test-useagent. UA-0046 is already claimed for claude. Read AGENTS.md, knowledge/INDEX.md, `python tools/useagent.py task show UA-0046`, work/evidence/ua-0043-visual-review.md, and UI/UX Pro Max skill + design-system/osblog/MASTER.md. Do not read full registry/history. Previous real Claude UI task UA-0043 completed and all 47 unit/SQL/component tests pass. Supervisor now has a PASSING two-test browser E2E on compiled SSR + isolated real PGlite SQL: login -> editor -> publish -> comment -> approve; public home and VI mobile docs axe pass. This is actual UI evidence, not production Vercel completion. Another worker owns docs/README; supervisor owns runtime/build/tests/config/deploy. You only own src/app, src/styles.css, src/main.tsx, public/assets, public/favicon.svg, design-system. Use apply_patch, no server/package/docs/README edits, no git/deploy.

Fix these evidence-backed issues (do not redesign branding):
1. Inspect actual screenshots test-results/editor-desktop.png and test-results/home-desktop.png (Read supports images). The editor sidebar labels/inputs are browser-default, cramped together; slug overflows preview URL. Style the actual `.editor-settings`/sidebar classes with block labels, full-width 44px minimum controls, consistent margins, border/background/focus/error styling, wrapping URL, proper sticky behavior. Admin editor h1 currently ~96px display size: make compact 28–36px, responsive so long titles do not dominate workspace. Mobile settings remain usable and no overflow.
2. Fonts look like fallbacks. Installed @fontsource-variable/source-serif-4 and @fontsource-variable/playfair-display exist; import correct local CSS for needed Latin+Vietnamese subsets in styles or main, confirm actual family names match tokens. Keep performance sensible and no remote font CDN.
3. Desktop home has hamburger simultaneously with full nav. Hide mobile toggle at desktop; preserve keyboard nav/focus at mobile.
4. SEO preview hardcodes osblog.thuanlyt.id.vn. Derive configured/current installation origin safely, SSR hydration deterministic. Public configured origin comes from import.meta.env.VITE_SITE_URL; actual preview link can use relative path; do not read secrets.
5. Docs headings currently have no IDs though links include #anchors. Add deterministic safe heading IDs, deduplicate headings, normalize duplicate first H1 so docs page doesn't repeat its displayed title; test anchor links. Keep safe Markdown, no raw HTML. Match conventional heading slugs, including Vietnamese characters. Source links in docs are being changed to absolute GitHub links by docs worker.
6. Add admin editor axe-friendly tab/tabpanel ID relationships (aria-controls and aria-labelledby) if missing; stable controls/labels. Keyboard shortcuts are optional; do not expand into unsupported features.
7. Create three small original repo-native SVG editorial cover illustrations under public/assets for intro articles (open ideas, Markdown, docs/code), using existing OSBlog palette/logo. They should be decorative vector art, no fake product screenshot claims or raster generation; report exact names for supervisor seed integration. Do not edit seed code yourself.

Use the existing tests and add focused regressions where useful. Do not change API names/routes/payloads or relax validation. Admin English UI with EN/VI editing tabs is acceptable to preserve scope; don't begin a whole translation rewrite. Supervisor will run compiled browser QA; you may run source tests, typecheck/lint. Don't run a build while supervisor's browser fixture is active unless explicitly coordinating; report source verification. Report via `python tools/useagent.py task report UA-0046 --agent claude --result completed ...` with files/checks; don't mark done. No phantom blocker: credentials are irrelevant to CSS work and real browser tests already authenticate through isolated fixtures. Keep final concise.
