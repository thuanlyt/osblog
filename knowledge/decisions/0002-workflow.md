# Decision 0002: Real worker and replay evidence

## Decision

Treat `antigravity`, `codex`, and `claude` as registered manual workers. Dispatch creates mailbox assignments and copyable outbox prompts. A result is accepted only when the worker's own report, evidence paths, review, and QA can be reproduced locally.

## Rationale

The requested usability test must exercise the actual UseAgent control plane without pretending an unavailable vendor runtime is live. Any GIF/MP4 or replay fixture must be labeled as replay/simulation unless it is a genuine screen recording.

## Consequences

The supervisor may dispatch all three identities but must report manual-runtime limitations honestly and never invent API calls, model execution, or external service evidence.
